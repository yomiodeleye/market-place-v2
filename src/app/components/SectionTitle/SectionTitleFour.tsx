const SectionTitleFour = ({ title }) => {
  return (
    <div className="section-title-container text-center space-mb--r80">
      <h2 className="section-title section-title--style-two">{title}</h2>
    </div>
  );
};

export default SectionTitleFour;
